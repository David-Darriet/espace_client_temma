<link href="{{ asset('css/navbar.css') }}" rel="stylesheet"/>

<nav>
    <div class="navbar">
        <div>
            <a href="{{ path('app_user_category_index') }}" class="navbar-brand mobile-invisible">
                <img src="{{ asset('img/logo/logo_temma_blanc.png') }}" alt="logo">
            </a>
            <a href="{{ path('app_user_category_index') }}" class="mobile-visible"><i class="fa-solid fa-house"></i></a>
        </div>
        <ul class="navbar-action">
            <li class="navbar-action__span">
                <a href="{{ path('app_user_show', {'id': app.user.id })}}">
                    <i class="fa-solid fa-circle-user"></i>
                    <span>{{ app.user.login }}</span>
                </a>
            </li>
            <li class="navbar-action__span">
                <a href="{{ path('app_logout') }}">
                    <i class="fa-solid fa-power-off"></i>
                    <span>Déconnexion</span>
                </a>
            </li>
        </ul>
    </div>
</nav>